//Variable is a container or memory locator which hold values
//it must not be a Number, key word or reserved statement else.g alert. Use conventional names
//javascript uses camoncasing pattern
let h = 1;  //Integar datatype involves use of digits, number

let o = "Blessing";   //String datatype involves characters, words or number in quotes e.g"5"

let m = false;        // Boolean datatype involves true or false value(binolean)

//Datatypes specify the type of data and size to be stored in a variable

let a = 15;

let b = 20;

let result = a + b;

alert("the result of 15 + 20 = " + result);

//when referencing a variable, dont quote it
//concantination= when a variable is referenced and initialized